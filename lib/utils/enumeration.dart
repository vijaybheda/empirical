enum MyPlaces { a, b, c }

enum ThemeType { light, dark }

enum Device { phone, tablet }

enum DateSort { none, asc, desc }

enum CommoditySort { none, asc, desc }

enum ResultSort { none, asc, desc }

enum ReasonSort { none, asc, desc }
